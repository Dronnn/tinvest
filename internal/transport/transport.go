// Package transport owns the gRPC connection and interceptors: Bearer auth,
// idempotency-aware retry, client-side rate limiting, timeouts, call-phase
// tracking, and x-tracking-id capture.
package transport
